services:
  - type: web
    name: qna-report
    runtime: node
    plan: free
    branch: main
    buildCommand: npm install
    startCommand: node server.mjs
    envVars:
      - key: NODE_VERSION
        value: "20"
      - key: TZ
        value: Asia/Seoul
